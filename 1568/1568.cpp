#include <bits/stdc++.h>

using namespace std;

typedef long long int llint;
//Qualquer divisão por número ímpar é válida
//Divisão por par -> ímpar. Sendo Ímpar > 2*par

int Soma(llint N){

    int i = 2;
}

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    long long int  N;

    while(cin >> N){
        cout << Soma(N) << endl;
    }

    return 0;
}
